interface BtnIcon {
    children?: React.ReactNode;
}

interface MainBtn {
    icon?: React.ReactNode;
    text?: string;
}

interface ProjFolder {
    heading?: string;
    tagList?: React.ReactNode;
    summary?: string;
    feature?: string;
}